{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    // Note: adjusted rootDir to contain stories, so utility functions can
    // be resolved across multiple story files.
    "rootDir": "src",
    "outDir": "dist",
    "declarationDir": "dist"
  },
  "include": ["src", ],
  "references": [
    { "path": "../color-utils" },
    { "path": "../data-components" },
    { "path": "../ui-components" }
  ]
}
