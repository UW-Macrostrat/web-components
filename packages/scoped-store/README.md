# Scoped store

A Jotai-based scoped store for React. This package provides a
`createScopedStore` function that allows you to create a store with a specific
scope. This is useful for managing state in a modular way, especially when you
have multiple instances of a component that need to maintain their own state. It
includes functions that allow you to sync Jotai and Zustand stores.
