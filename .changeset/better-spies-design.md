---
"@macrostrat/scoped-store": major
"@macrostrat/data-components": minor
"@macrostrat/mapbox-react": minor
"@macrostrat/data-sheet": minor
---

Create a `@macrostrat/scoped-store` library:
- Move Jotai scope and enhancements to a separate package (formerly part of `@macrostrat/data-components`).
- Add extensions for Zustand coordination (`ZustandStoreProvider`, `useZustandSelector`, `useZustandStoreAPI`).
