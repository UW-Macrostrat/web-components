---
"@macrostrat/mapbox-react": minor
---

Add `useLinkedMapCameras` / `linkMapCameras` / `copyMapCamera`, which lock several maps' cameras together using the free-camera API

`MapboxMapProvider` accepts an `inherit` prop, so a second map can be managed within an existing map context
