---
"@macrostrat/column-views": minor
---

Add `useAnimatedAgeWindow`: animates a column/correlation-chart's rendered `t_age`/`b_age` for smooth pan-and-contract navigation (constant `pixelScale`), driving the existing clipping + zig-zag unit edges. Click a timescale interval to navigate.
