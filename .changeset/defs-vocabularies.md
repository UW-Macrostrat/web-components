---
"@macrostrat/data-provider": minor
---

Lithology attributes (`/defs/lithology_attributes`) and timescales in the store,
as `lithAttributes` and `timescales`, with `useLithAttributes`,
`useTimescales`, `fetchLithAttributes` and `fetchTimescales`.
