---
"@macrostrat/data-sheet": patch
---

A fill-drag ends when the mouse button is released: the anchor no longer fills later selections, the fill is reported through `onEdit`, and shrinking a drag restores the rows it leaves
