---
"@macrostrat/column-views": patch
---

`Proportion`, `AgeLabel` and `getAge` are re-exported from
`@macrostrat/data-components`, where the interval tag's proportion prefix and age
details now live; the surface calibration tags use `IntervalTag`'s `proportion`.
