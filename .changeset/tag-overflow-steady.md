---
"@macrostrat/data-components": patch
---

A one-line tag row no longer alternates between a tag and "and n more": hidden tags keep their full width, the row re-measures only when its width changes, and the first tag always stays, clipped if need be
