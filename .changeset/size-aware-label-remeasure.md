---
"@macrostrat/ui-components": minor
---

`SizeAwareLabel` accepts a `remeasureKey` prop folded into its fit-measurement dependencies, so a consumer can force a re-fit (e.g. when an animation settles) without the label text changing.
